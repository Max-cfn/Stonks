[project]
name = "stonks-backend"
version = "0.1.0"
description = "Stonks — Backend FastAPI (stub Phase 1, implémentation Phase 2)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
